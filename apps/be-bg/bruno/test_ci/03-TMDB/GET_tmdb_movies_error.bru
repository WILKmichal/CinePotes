meta {
  name: GET /tmdb/movies (error - no ids)
  type: http
  seq: 6
}

get {
  url: {{URL}}/tmdb/movies
  body: none
  auth: none
}

tests {
  test("Status code is 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
  
  test("Error message present", function() {
    expect(res.body.message).to.include('ids est requis');
  });
}
