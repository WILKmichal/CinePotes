meta {
  name: GET /nats-exemple/ask
  type: http
  seq: 4
}

get {
  url: {{URL}}/nats-exemple/ask?nom=Bruno
  body: none
  auth: none
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
